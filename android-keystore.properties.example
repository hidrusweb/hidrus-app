# Copie para android/keystore.properties e preencha (android/keystore.properties está no .gitignore).
# Caminho do .jks em relação à raiz do app Expo (pasta do package.json):
storeFile=@hidrusweb__hidrus-app.jks
storePassword=
keyAlias=
keyPassword=
